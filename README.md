# elytra-collector-runner

Scheduled data-collection jobs for a private project. This repo holds
only the two workflow files; code and data live in private repos
reached through scoped deploy keys. Workflow run status doubles as the
pipeline's public health signal.
